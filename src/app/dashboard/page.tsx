import { DashboardShell } from "@/components/dashboard/dashboard-shell";

// Thin route entry point for the authenticated dashboard shell.
export default function DashboardPage() {
  return <DashboardShell />;
}
